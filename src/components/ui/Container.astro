---
import { twMerge } from "tailwind-merge";

type ContainerProps = {
  class?: string;
};

const { class: className } = Astro.props as ContainerProps;
---

<div class={twMerge("mx-auto max-w-7xl px-4 md:px-16", className)}>
  <slot />
</div>
