---
import SectionWrapper from "@/components/ui/SectionWrapper.astro";
import Text from "@/components/ui/Text.astro";
import Divider from "@/components/ui/Divider.astro";

import { getLangFromUrl, useTranslations } from "@/i18n/utils";
import DownloadCV from "@/components/ui/DownloadCV.astro";
import Socials from "@/components/ui/socials.astro";

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const greet = t("hero.greet");
const name = t("hero.name");
const role = t("hero.role");
const rest = t("hero.rest");
const description = t("hero.description");
---

<SectionWrapper
  class="relative max-w-6xl min-h-[calc(100vh-75px)]"
  title="hero"
  divider={false}
>
  <div class="flex flex-col justify-center gap-2 mb-32">
    <Text as="h1" variant="hero">
      <span class="text-2xl text-foreground/70">
        {greet}
      </span>
      <br />
      {name}
      <br />
      <span class="text-accent"> {role}</span>
      <br />
      {rest}
    </Text>
    <Text as="p" variant="lead" class="text-lg">
      {description}
    </Text>
    <Divider />
    <div class="flex gap-2">
      <Socials showName={false} />
      <DownloadCV />
    </div>
  </div>
</SectionWrapper>
